export interface QueryParams
{
	[ key : string ] : any;
}

export function Parse( param : string ) : QueryParams 
{
	//Sort
	//Pagination
	//Filter
		// value
		// comparisons
		// logical statements
		// subsets
	//populate

	return {} as QueryParams;
}